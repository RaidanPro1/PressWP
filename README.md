# 🇾🇪 منصة YemenJPT للصحافة الاستقصائية

**Yemen Journalist Pre-trained Transformer (YemenJPT)** هي منصة متكاملة ومفتوحة المصدر مصممة خصيصًا لتمكين الصحفيين والمؤسسات الإعلامية في اليمن. توفر المنصة مجموعة شاملة من أدوات التقصي مفتوح المصدر (OSINT)، التحقق من المعلومات، التحليل بمساعدة الذكاء الاصطناعي، والتعاون الآمن، كل ذلك في بيئة ذاتية الاستضافة تضمن السيادة الرقمية وحماية البيانات.

---

## ✨ الميزات الرئيسية

- **النواة المعرفية (AI Core):** مساعد ذكي (YemenGPT)، تفريغ صوتي للهجات اليمنية، تحليل صور، وتلخيص نصوص.
- **ترسانة OSINT:** مجموعة أدوات مدمجة للبحث المتقدم، تحليل الشبكات الاجتماعية، تتبع البصمة الرقمية، والتحقق من المحتوى المرئي.
- **مختبر المؤشرات (IndiLab):** نظام إنذار مبكر يعتمد على تحليل المؤشرات غير المباشرة مثل التشويش على GPS وتتبع حركة الطيران.
- **مساحة عمل تعاونية:** إدارة آمنة للمشاريع والمهام، ومشاركة الملفات، ونقاشات الفريق.
- **بوابات متخصصة:** بوابات لإدارة الأخبار، المشاريع، رصد الانتهاكات، والتدريب.
- **أمان وسيادة رقمية:** تصميم قائم على الاستضافة الذاتية (Self-hosted) يضمن بقاء جميع بياناتك الحساسة على خوادمك الخاصة.
- **إدارة شاملة:** لوحة تحكم إدارية قوية لمراقبة النظام، إدارة الأدوات، والمستخدمين.

---

## 🛠️ التقنيات المستخدمة

- **الواجهة الأمامية:** Angular, TypeScript, Tailwind CSS
- **البنية التحتية:** Docker, Docker Compose
- **البوابة العكسية:** Traefik
- **الذكاء الاصطناعي المحلي:** Ollama
- **قاعدة البيانات:** PostgreSQL
- **الأتمتة:** n8n
- **إدارة الحاويات:** Portainer
- **خادم الإشعارات:** Node.js, Express, Telegram Bot API

---

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من توفر المتطلبات التالية:

1.  **خادم (Server):** خادم VPS أو Dedicated Server بنظام تشغيل Linux (يفضل Ubuntu 22.04).
2.  **اسم نطاق (Domain Name):** اسم نطاق صالح وموجّه إلى عنوان IP الخاص بخادمك.
3.  **Docker و Docker Compose:** يجب أن يكون Docker و Docker Compose V2 مثبتين على الخادم.
4.  **حساب Cloudflare:** لإدارة DNS وتوليد شهادات SSL تلقائيًا.

---

## 🚀 دليل التثبيت

اتبع هذه الخطوات لتثبيت وتشغيل المنصة بالكامل.

### الخطوة 1: استنساخ المستودع

اتصل بالخادم الخاص بك عبر SSH وقم باستنساخ هذا المستودع:

```bash
git clone https://github.com/your-repo/yemenjpt.git
cd yemenjpt
```

### الخطوة 2: إعداد ملف البيئة

انسخ ملف `.env.example` إلى ملف جديد باسم `.env`. هذا الملف سيحتوي على جميع الإعدادات والمتغيرات السرية الخاصة بك.

```bash
cp .env.example .env
```

افتح الملف الجديد باستخدام محرر النصوص المفضل لديك (مثل `nano`) واملأ جميع الحقول المطلوبة:

```bash
nano .env
```

**ملاحظة هامة:** املأ جميع الحقول بعناية، خاصة `DOMAIN`, `SERVER_IP`, و `CF_TOKEN`. استخدم كلمات مرور قوية وفريدة.

### الخطوة 3: تشغيل سكربت التثبيت

بعد حفظ ملف `.env`، قم بإعطاء صلاحيات التنفيذ لسكربت التثبيت ثم قم بتشغيله.

```bash
chmod +x setup.sh
./setup.sh
```

سيقوم السكربت بالآتي:
- التحقق من وجود Docker و Docker Compose.
- التحقق من وجود ملف `.env`.
- إنشاء المجلدات اللازمة لتخزين البيانات الدائمة (مثل قواعد البيانات والملفات).
- تشغيل جميع خدمات المنصة في الخلفية باستخدام `docker compose up -d`.

### الخطوة 4: الوصول إلى المنصة

بعد اكتمال التثبيت بنجاح، يمكنك الوصول إلى المنصة والخدمات المرتبطة بها عبر الروابط التالية (استبدل `yourdomain.com` بنطاقك):

- **المنصة الرئيسية:** `https://yourdomain.com`
- **لوحة تحكم الوصول السريع (Dashy):** `https://portal.yourdomain.com`
- **إدارة النظام (Portainer):** `https://sys.yourdomain.com`
- **مراقبة الأداء (Glances):** `https://glances.yourdomain.com`
- **مستودع الكود (Gitea):** `https://git.yourdomain.com`
- **الأتمتة (n8n):** `https://auto.yourdomain.com`

---

## 🔧 الإدارة والصيانة

- **إيقاف المنصة:** `docker compose down`
- **إعادة تشغيل المنصة:** `docker compose restart`
- **عرض سجلات خدمة معينة:** `docker compose logs -f <service_name>` (مثال: `docker compose logs -f frontend`)
